LIBRARY MANAGEMENT SYSTEM
admin {admin_ID, admin_usernaem, admin_password};
students {s_ID, s_fullname, s_course_section, s_username, s_password}
books {ISBN, book_title, book_author, book_category, book_published }
request_logs {rl_ID, rl_date_time, s_ID(FK), ISBN(FK)};
book_borrowed {borrow_ID, date_borrowed, borrow_status, ISBN(FK), s_ID};
book_returned {return_ID, date_returned, s_ID(FK), ISBN(FK) };

SELECT request_logs.ISBN, books.book_title, books.book_author, books.book_category, students.s_ID, request_logs.rl_dateTime FROM ((request_logs INNER JOIN books ON request_logs.ISBN = books.ISBN) INNER JOIN students ON request_logs.s_ID = students.s_ID);

// 0 means pending
// 1 means book_borrowed or approve
// 2 means decline
// 3 means returned

Set @autoid :=0;
update employee set id = @autoid := (@autoid+1);
alter table employee Auto_increment = 1

// alter sa part na borrowedbooks student and insert requestlogs in requestlogs admin